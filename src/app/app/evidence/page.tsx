import { ExternalLink } from "lucide-react";
import { evidence } from "@/lib/academic-data";

export default function EvidencePage() {
  const sources = evidence.filter((source) => source.pathwayId === "ucsd-data" || source.institutionId === "coc");
  return <div className="production-page evidence-page"><header className="production-page-header"><h1>Evidence behind your plan</h1><p>Every important academic claim carries its source and verification state.</p></header><div className="evidence-key"><span className="verified">Verified</span><span className="suggestion">Planning suggestion</span><span className="review">ASSIST or counselor confirmation needed</span></div><div className="evidence-list">{sources.map((source) => <article key={source.id}><div><span className={`source-status ${source.status}`}>{source.status === "verified" ? "Verified source" : "Confirmation needed"}</span><h2>{source.title}</h2><p>{source.note}</p></div><dl><div><dt>Effective period</dt><dd>{source.effectiveYear}</dd></div><div><dt>Retrieved</dt><dd>{source.retrievedAt}</dd></div><div><dt>Provenance</dt><dd>{source.provenance.replaceAll("_", " ")}</dd></div></dl><a href={source.url} target="_blank" rel="noreferrer">Open official source <ExternalLink /></a></article>)}</div><div className="evidence-disclaimer"><strong>Important limitation</strong><p>Waylo validates internal prerequisite order and scheduling against its reviewed dataset. Exact COC-to-UCSD articulations still require confirmation in the applicable ASSIST agreement or with a counselor where marked.</p></div></div>;
}
